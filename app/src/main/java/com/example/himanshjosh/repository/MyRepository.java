package com.example.himanshjosh.repository;

import com.example.himanshjosh.loign.responce.getrequest.ModelLoginResponce;
import com.example.himanshjosh.network.NetworkListener;
import com.example.himanshjosh.network.SafeApiRequest;

import retrofit2.Response;

public class MyRepository extends SafeApiRequest {

   // private NetworkListener networkListener;

  /*  public MyRepository(NetworkListener networkListener) {
        this.networkListener = networkListener;
    }

    public Response<ModelLoginResponceSignup> loginUser(String email, String password)
    {
        return networkListener.loginUser(email,password);
    }
*/





}
